import APIService from '@services/APIService'

const state = {
    // TODO: a list of states related to Senators
}

const getters = {
    // TODO: define methods to acces state value
}

const actions = {
    // TODO: define actions
}

const mutations = {
    // TODO: define mutations to redefine state value
}

export default {
    state,
    getters,
    actions,
    mutations
}