version: '3'

services:
  db:
    image: postgres
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:5432
    volumes:
      - .:/code/citizencaucus
    ports:
      - "5432:5432"
    depends_on:
      - db